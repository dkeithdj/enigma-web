---
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";
import { auth } from "@/firebase/server";
import Layout from "@/layouts/Layout.astro";
/* Get session cookie */
if (!Astro.cookies.has("session")) {
  return Astro.redirect("/signin");
}
const sessionCookie = Astro.cookies.get("session").value;
const decodedCookie = await auth.verifySessionCookie(sessionCookie);
const user = await auth.getUser(decodedCookie.uid);

if (!user) {
  return Astro.redirect("/signin");
}
---

<Layout title="About">
  <section class="py-12 p-4 md:mx-20">
    <h1 class="text-5xl text-center pb-8">
      About: <span class="font-semibold">The Organization</span>
    </h1>
    <Card className="mb-8">
      <CardHeader>
        <CardTitle>Vision</CardTitle>
      </CardHeader>
      <CardContent>
        <p class="text-justify">
          We aspire to be a community of <span class="font-bold"
            >exceptional individuals</span
          > equipped with the skills, knowledge, and mindset to excel in their
          respective fields of expertise, pioneering a path toward a future
          where technology is harnessed to make a profound and purposeful
          impact, driving progress forward.
        </p>
      </CardContent>
    </Card>
    <Card className="mb-8">
      <CardHeader>
        <CardTitle>Mission</CardTitle>
      </CardHeader>
      <CardContent>
        <p class="text-justify">
          Our mission is to create a community of exceptional technology
          students by providing resources, training, and support. We empower
          members with the skills, knowledge, and mindset to drive technological
          advancements. Through innovative programs, we foster a culture of
          collaboration, lifelong learning, and ethical responsibility, shaping
          a generation of professionals who will revolutionize industries and
          contribute to a better future.
        </p>
      </CardContent>
    </Card>
    <Card>
      <CardHeader>
        <CardTitle>Goals</CardTitle>
      </CardHeader>
      <CardContent>
        <p class="text-justify">
          Our mission is to create a community of exceptional technology
          students by providing resources, training, and support. We empower
          members with the skills, knowledge, and mindset to drive technological
          advancements. Through innovative programs, we foster a culture of
          collaboration, lifelong learning, and ethical responsibility, shaping
          a generation of professionals who will revolutionize industries and
          contribute to a better future.
        </p>
      </CardContent>
    </Card>
  </section>
  <!-- <section class="py-12 p-4 md:mx-20">
    <h1 class="text-5xl text-center pb-8">
      About: <span class="font-semibold">The Logo</span>
    </h1>
    <LogoProcess client:load />
  </section> -->
</Layout>

 test test test test test
